This is a demonstration plugin, showing basic setup.

build.js is called by the build tool, and can be used to preprocess data.
includes_head.html is a file that is copied into the head section of index.html by the build tool.
includes_body.html is a file that is copied into the body section of index.html by the build tool.

src/plugin.js is the code that defines 2 function that are called when the site it loaded and when the page is read.
src/plugin.css is some CSS used to enhance the text.